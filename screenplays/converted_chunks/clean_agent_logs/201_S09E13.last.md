已按要求只清理了 [201_S09E13.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/201_S09E13.md)。

我做的改动仅限解析器相关格式：
- 把所有裸露的 `Scene:` + 下一行地点，统一成 `*[Scene: ...]*`
- 把 `Opening Credits`、`Ending Credits` 统一成 `*[...]*`
- 保留了对白原文，不改措辞、不改标点、不跑 parser 或校验命令

短报告：
- 已修复的模式：15 处裸 `Scene:` 场景包装，2 处 standalone credits
- 保留未改的歧义项：片尾对白中的 `*[to the Touching guy]*` 仍保留原样，因为它夹在对白内部，改成独立 action 或改 wrapper 类型都可能超出“只做安全格式修复”
- `END` 之后的导航/站点说明内容未处理；按规范应视为正文外内容，但你这次没有要求删除它们，所以保持不变