已按要求只清洗了 [195_S09E07.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/195_S09E07.md)。

我做的都是 parser-facing Markdown 规范化：
- 把所有 `SCENE:` 加后续场景描述合并成 `*[Scene: ...]*`
- 把独立的 `OPENING CREDITS`、`Commercial Break` 规范成 `*[...]*`
- 把若干 `Speaker:*(...)` 修成 `Speaker: *(...)`，不改对白字词
- 把独立 `END` 规范成 `End`，便于后续把后面的页脚内容视为非正文

保留不动的模糊处：
- `? Previous *[906]* *[907]* *[908]* Next ?` 及其后页脚内容仍在 `End` 之后，未删除
- 没有发现需要安全拆分的明确“粘连多说话人/场景”案例；含冒号的台词如 `My experience:`、`my parents:` 保持原样，避免误切分

未运行任何 parser 脚本或验证命令。