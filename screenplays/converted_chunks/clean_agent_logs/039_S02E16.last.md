Edited [039_S02E16.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/039_S02E16.md).

Fixed:
- Wrapped standalone `OPENING TITLES` / `CLOSING CREDITS` as stage context.
- Converted standalone actions from `*[...]*` to `*(...)*`.
- Split glued dialogue/action boundaries into separate dialogue and action lines.
- Kept delivery-style inline actions such as `*(reluctantly)*`, `*(quietly)*`, and `*(to Rachel)*`.

No ambiguous cases were changed or left needing review. I did not run parser scripts or validation commands. `END` remains as-is, and there is no content after it.