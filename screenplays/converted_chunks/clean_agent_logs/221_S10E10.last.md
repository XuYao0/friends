已按 `clean-friends-screenplay` 规范只清理了 [221_S10E10.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/221_S10E10.md)。

本次修改只涉及解析器相关 Markdown 格式，没有改写对白措辞，也没有运行 parser/validation。修复内容包括：将 `OPENING CREDITS`、4 处 `TIME LAPSE`、`COMMERCIAL BREAK` 规范成独立的 stage-context 行；将结尾 `THE END` 规范成独立 `End`，便于解析在此停止；修正了 `Phoebe :` 的说话人格式、`Monica *(puzzled)*:` 的对话包装格式，以及一处明显的对话内动作包装 `*[looking outside the window]* -> *(looking outside the window)*`。

保留未动的歧义点：
- 若干台词中的内联动作连写形式，例如 `...so... *(back to Central Perk)* ...`、`*(To Rachel)* *(Starts to cry)*`，边界可读但不需要强拆。
- `?[ Previous ... ]` 及后续站点页脚内容位于 `End` 之后，按规范视为非正文，未删除。
- 文中若干 OCR/转码异常字符，例如 `Who抎`、`Y択now`、`I抎`，这不属于本次“只做格式规范化”的安全修改范围，因此保持原样。