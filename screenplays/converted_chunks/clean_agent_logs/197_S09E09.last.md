已编辑 [197_S09E09.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/197_S09E09.md)。

本次只做了面向解析器的 Markdown 归一化，保留了台词 wording 不变：
- 把整集的 `SCENE:` 两行结构统一改成 `*[Scene: ...]*`
- 把 `OPENING CREDITS` 规范成 `*[Opening Credits]*`
- 修复了几处明显损坏的 scene wrapper，比如带残缺 `)` 的场景描述和 `Monica and Chandler's apartment " continuing action.` 这一处
- 把明确的 `Speaker:*(...)` 粘连改成 `Speaker: *(...)`，未改动台词文本

保留未动的疑点：
- `End` 后的页脚/导航内容按要求未处理
- 开头大量站点宣传与导航文本位于正文前，是否应在后续单独清理为“非故事头部噪音”还需要人工决定；这次按“歧义不改”保留
- 未运行 parser 脚本或验证命令，符合你的限制