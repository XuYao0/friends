已只修改 [199_S09E11.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/199_S09E11.md)。

处理内容是面向解析器的 Markdown 归一化：把所有拆开的 `Scene:` 两行场景合并为 `*[Scene: ...]*`，把 `Opening credits` / `Closing credits` 规范成独立舞台上下文，修正了明显破损的说话人格式如 `Phoebe Thank you...`、`Joey:: ...`，并统一了 `Speaker:*(...)` 为 `Speaker: *(...)`，未改动对白措辞。另修复了一处明显错位的场景标题，把 `Gavin/Rachel` 那段错误场景包裹改成了 `*[Scene: Ralph Lauren]*`。`END` 规范成了 `End`，其后的尾部导航/站点说明保留未删。

保留未动的模糊项只有两类：开头首场戏之前的宣传/站点文本，以及 `End` 之后的页脚导航文本。这两部分没有按你的要求做删除或进一步结构化。未运行任何 parser 脚本或验证命令。