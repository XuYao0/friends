已按要求只清理了 [191_S09E03.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/191_S09E03.md)，没有运行任何 parser 脚本或校验命令。

本次修复只涉及面向解析器的 Markdown 格式：
- 将全文 `SCENE:` 行规范为独立的 `*[Scene: ...]*` stage 行。
- 将 `INTRO` 规范为 `*[Opening Credits]*`。
- 将 `CUT TO: Monica and Chandler's apartment.` 规范为 `*[Cut to: Monica and Chandler's apartment.]*`。
- 修复两处明显损坏的说话人标记：`Chandler.` -> `Chandler:`，`Mary Ellen Wait, ...` -> `Mary Ellen: Wait, ...`。

保留未动的歧义项：
- 文件末尾没有独立的 `End`，因此后面的导航/站点说明文本未删除。
- 像 `Ross: *(Enters from his bedroom)*Who the hell was that?!` 这类可能还可继续微调空格的地方，我保持原样，避免碰到对白原文。