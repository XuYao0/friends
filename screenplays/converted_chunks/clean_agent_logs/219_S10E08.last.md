已编辑 [219_S10E08.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/219_S10E08.md)。

本次只做了确定无歧义的 parser-facing 格式修复，未运行任何 parser 或校验命令，也未改动对白措辞。修复项是：
- 将独立 credits `OPENING CREDITS` 规范为 `*[Opening Credits]*`
- 将独立 credits `COMMERCIAL BREAK` 规范为 `*[Commercial Break]*`
- 修复一处明显损坏的动作包裹：`(Rachel comes out of her apartment}` -> `*(Rachel comes out of her apartment)*`

保留未改的含糊项：
- `THE END` 保持原样。它更像终止标记而非普通场景/动作行，强行改成 `*[...]*` 可能改变下游停读语义。
- 若干已可被解析的场景/切换行（如 `*[cut to ...]*`、`*[time lapse ...]*`、个别 scene 行末无句号）未改，只因边界已经明确，继续“美化”会增加不必要的 wording/punctuation 变动。