已只修改 [196_S09E08.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/196_S09E08.md)。

处理内容：
- 把裸场景标题规范成 `*[Scene: ...]*`。
- 把 `Opening Credits`、`Commercial Break` 等独立片头/分隔标记规范成 `*[...]*`。
- 把明显塞进 speaker 标签里的动作拆成独立 `*(...)*` 行，再保留原对白为 `Speaker: utterance`。
- 修了少量明显的对白前空格/包装格式问题，比如 `Monica:*(laughing)*`。

保留未强改的模糊项：
- 开头 `End` 之前的站点导览/售卖信息元数据未重写，只保留原样。
- 少数内容语义上更像转录注释而非标准场景/动作的行，例如 `*[Transcriber note: ...]*`，保持不变。
- 没有强行改写任何不够确定的 speaker 归属或台词内容。

约束遵守情况：
- 没有运行 parser 脚本。
- 没有运行校验命令。
- `End` 之后的 footer/navigation 内容未处理。