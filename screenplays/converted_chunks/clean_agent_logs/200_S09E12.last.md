已按要求只规范化了 [200_S09E12.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/200_S09E12.md)。

处理内容：
- 把所有 `Scene:` 加下一行场景描述，统一成 `*[Scene: ...]*`。
- 把 `OPENING CREDITS`、`ENDING CREDITS` 规范成独立 stage-context 行。
- 把明显粘连的动作/说话拆开成独立逻辑行：
  - `Gavin: Mom, I'll call you later. Yeah. *(hangs up)**(to Rachel)* Yes?`
  - `Rachel: Oh, ok, thank you. *(Molly leaves)**(to Monica)* Do you see what all the guys see in her?`
- 把独立转场 `cut to Ross ...` 规范成单独的 stage-context 行。
- 台词 wording 未改。

保留未动的模糊处：
- `END` 之后的导航/站点页脚内容仍保留原样；按规范应视为正文外内容，但你没有要求删除。
- 若干台词内联动作的空格/排版不完全统一，例如 `Phoebe:*(relived)* ...`、`Chandler:*(In a helium voice...)`；这些不影响 speaker 边界，且为避免过度改动，未处理。
- `*[cut to Ross ...]*` 的大小写保留原文，没有擅自改成 `Cut to ...`。

未运行 parser 脚本，也未运行任何验证命令。