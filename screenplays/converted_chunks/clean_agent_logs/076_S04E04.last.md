Edited [076_S04E04.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/076_S04E04.md).

Fixed:
- Normalized 33 `Mr. Treeger::` speaker labels to `Mr. Treeger:`.
- Split Monica’s glued malformed action from dialogue and repaired it as `*(...)*`.
- Normalized `OPENING CREDITS`, `COMMERCIAL BREAK`, and `CLOSING CREDITS` to stage-context lines.
- Normalized final `END` to standalone `End`.

Left ambiguous:
- `towel*(毛巾)* covered portion` was left unchanged because it may be a translation/gloss rather than an action, and changing it could alter dialogue text.

No parser scripts or validation commands were run. There is no content after `End`.