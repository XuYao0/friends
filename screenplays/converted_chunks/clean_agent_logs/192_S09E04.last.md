已按要求只清理了 [192_S09E04.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/192_S09E04.md)。

我做的都是 parser-facing 格式规范化：把 `SCENE:` 行改成 `*[Scene: ...]*`，把明确的独立动作改成 `*(...)*`，把 `Opening Credits` 规范成 `*[Opening Credits]*`，并把 `SCENE: Central Perk, Monica enters.` 拆成单独 scene 行和 action 行；另外去掉了明显的 HTML 残留 `/p>`。对白措辞未改，也没有运行 parser 或任何校验命令。

保留未动的歧义项：
- `Mike *(Paul Rudd)* enters.`：带嵌套强调标记，若强行包成 `*(...)*` 容易引入新的解析歧义。
- `Joey: And who's fault is that? *[note, see 306, etc.]*`：像是外部注记，是否应移出对白不够确定。
- `End` 之后的 footer/navigation 内容保持原样，未删除。