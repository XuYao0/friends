Edited [128_S06E09.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/128_S06E09.md).

Fixed:
- Normalized `Opening Credits`, `Commercial Break`, and `Closing Credits` as stage context.
- Repaired malformed `Janine *[to Chandler]*: Hey.` into parser-friendly dialogue with inline action.
- Converted obvious glued `*[...]*` inline actions inside dialogue to `*(...)*`.
- Converted the action-only `Chandler: *[mocks that last comment]*.` line into an independent action.

Left unchanged:
- Standalone bracketed `*[...] *` stage/action context lines that were already isolated and could be treated as parser scene/stage context.
- No content after standalone `End` needed removal.

I did not run parser scripts or validation commands.