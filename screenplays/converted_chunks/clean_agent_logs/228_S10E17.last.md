已按要求只清理了 [228_S10E17.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/228_S10E17.md)。

本次修复只涉及面向解析器的 Markdown 格式，没有改动任何对白措辞，也没有运行 parser 或验证命令。具体做了这些安全归一化：
- 把 `OPENING CREDITS`、`COMMERCIAL BREAK` 规范为 `*[...]*` 场景/阶段上下文。
- 拆开了 4 处粘连的 `Cut to ...` 转场，把转场改为独立的 `*[Cut to ...]*` 行，并把相邻动作保留为独立 `*(...)*` 行。
- 保留了所有现有说话人台词和 inline action 原文。

短报告：
- 已修复的可疑模式：独立字幕行 2 处，粘连转场/动作 4 处。
- 留待人工判断的歧义项：`THE END` 未改。它更像终止标记而不是正文场景行；按“歧义则不动”保留原样。