已按 `clean-friends-screenplay` 规范只清理了 [223_S10E12.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/223_S10E12.md)。

本次只做了面向解析器的 Markdown 规范化，没有运行 parser 或校验命令，也没有改动台词 wording。主要修复了：
- 把独立的 `OPENING CREDITS`、`COMMERCIAL BREAK`、`THE END` 规范成独立的 stage-context 行 `*[...]*`
- 把几处独立的 `Cut to ...` / `we cut to ...` 转场从 `*(...)*` 统一成 `*[...]*`
- 把一处粘连的动作+转场拆开：
  - `*(She walks down the aisle with the groomsman. We cut to inside Central Perk...)*`
  - 改为独立 action 行 + 独立 cut 行
- 修正一处会干扰说话人解析的格式：
  - `Monica *(rolling her eyes)*: oh God.`
  - 改为 `Monica: *(rolling her eyes)* oh God.`

保留未改、建议人工复核的模糊点：
- 若干台词里的 `*(pause)*.`、`*(she reads a note)*.`、`*(to Phoebe and Mike)*.` 这类标点位置不够整齐，但仍可解析为同一说话人的 inline action；为避免误伤原句节奏，本次未动。
- 其余独立 `*(...)*` 多数是动作而不是场景/转场，按规范保持不变。