已处理 [198_S09E10.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/198_S09E10.md)。

本次只做了解析器友好的 Markdown 规范化，未运行任何 parser 或验证命令，也未改动对白措辞。修复内容主要是：
- 将 `SCENE:` + 下一行场景描述合并为 `*[Scene: ...]*`
- 将 `Opening Credits`、`Closing Credits` 规范为独立舞台上下文行
- 将 `Flashback to ...`、`back to ...`、电话两地切换说明规范为独立 `*[...]*` 上下文行
- 补齐明显粘连的说话人格式，如 `Joey:*(...)` -> `Joey: *(...)`、`Monica:What???` -> `Monica: What???`
- 拆开两处明显粘连的动作/对白片段，避免一行里混入多个逻辑事件

保留为人工复查的含糊项：
- 开头 Phoebe 朗诵诗歌后的几行续行文本仍保持原样，未强行归并为同一说话人单行
- `*[Scene change back an forth: ...]*` 中原文拼写 `an forth` 保留，因这不属于纯格式修复
- `End` 之后的页脚/导航内容未删除；按你的要求未继续清理，只在处理时将 `End` 视为正文终点并停止故事区规范化