已编辑 [183_S08E18.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/183_S08E18.md)。

本次只做了解析器导向的 Markdown 规范化，没有运行任何 parser 或校验命令，也没有改动对白措辞。修复内容是：
- 将独立 credits 行包装为场景/上下文行：`Opening Credits`、`Commercial Break`、`Ending Credits` -> `*[...]*`
- 将明确属于场景切换的独立 `Cut to ...` 行从 `*(...)*` 统一为 `*[...]*`
- 保留了 `End`，未处理其后的任何内容，因为该文件在 `End` 处结束

留作复核的歧义项：
- `Parker: ... {Transcriber’s Note: Interestingly Alec Baldwin was born in Massapequa.}` 这一行含转录者注记，但它嵌在对白行内；在不改写内容且不做推断的前提下，我保持原样。