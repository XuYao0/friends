已只规范化 [205_S09E17.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/205_S09E17.md)。

修复内容：
- 将独立的 `Opening credits` 规范为 `*[Opening Credits]*`
- 将独立的 `Ending Credits` 规范为 `*[Ending Credits]*`
- 修复一处明显畸形对白包装：
  `Rachel: *(threatening Joey with a scrunchy)*: ...`
  改为
  `Rachel: *(threatening Joey with a scrunchy)* ...`
- 将独立结尾标记 `END` 统一为 `End`，便于按规范在此停止正文

保留未改的含糊处：
- 未改若干台词内的非常规标点、大小写和拼写，如 `uhu uhu`、`She`/`Turns` 大写、`the the`、`Chandlers opens`、`one americans` 等，因为这不属于安全的 parser-facing 边界修复
- 未改 `End` 之后的导航和页脚内容；它们应视为非正文并在解析时忽略

未运行解析器脚本或校验命令。