Used `clean-friends-screenplay` on [053_S03E06.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/053_S03E06.md).

Working-tree formatting fixes present:
- Normalized `OPENING CREDITS`, `COMMERCIAL BREAK`, `CLOSING CREDITS` as `*[...]*`.
- Wrapped the standalone Joey/Monica stage action.
- Split/cleaned the glued `br> Rachel:` speaker line.
- Fixed spacing before an inline action.
- Normalized final standalone `End`.

Ambiguous left unchanged:
- The malformed pool-table action line with nested/broken parentheses near `Lisa almost lost it there`.
- Mojibake/OCR-looking text such as `慍handler`, `抎`, etc., because dialogue wording must be preserved exactly.

No parser scripts or validation commands were run.