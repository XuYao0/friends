已按要求只清理了 [220_S10E09.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/220_S10E09.md)，没有运行任何 parser 脚本或校验命令。

本次修复的都是 parser-facing Markdown 格式问题：
- 修复开头 scene wrapper 的明显粘连：`couch.Monica` 拆成 `couch. Monica`
- 将 Monica 那句结尾粘连的 `{she leaves)` 拆成独立动作行并修复为 `*(she leaves)*`
- 将 `OPENING CREDITS`、`TIME LAPSE`、`COMMERCIAL BREAK` 规范成独立的 `*[...]*` 舞台/场景上下文行
- 拆开 `Monica: Ok, thank you.*(the man leaves)* Uh, well this is it...` 这种对白/动作/对白粘连
- 规范 `Phoebe *(walking ...)*:` 为 `Phoebe: *(walking ...)* ...`
- 修复一处对白里动作前缺少空格的粘连
- 合并两处被错误断开的单句对白，避免同一 utterance 跨多行

保留未动、建议人工复核的模糊点：
- `*(Chandler takes Monica's hand, and gets serious)* Look, before we sign anything...` 这一处后半句缺少明确 speaker 标签；虽然上下文很像 Chandler，但补 speaker 会引入推断，所以保留原样
- 文件末尾的 standalone `end` 也保留原样，没有主动改成 `End`，因为你要求模糊情况不强行改动