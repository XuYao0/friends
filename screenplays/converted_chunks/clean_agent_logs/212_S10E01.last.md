已按要求只清洗了 [212_S10E01.md](/home/xuyao/friends/screenplays/converted_chunks/老友记_1-10季剧本_by_episode/212_S10E01.md)。我没有运行 parser 脚本或验证命令，也没有改动对白措辞。

本次修复的是纯解析边界问题：把独立的 `OPENING CREDITS` / `COMMERCIAL BREAK` 规范成 `*[...]*`，把几处明显属于场景切换的独立括号行改成 `*[...]*`，修复了 `*[Scene: Ross's apartment.Someone's ...]*` 这个明显损坏的 scene wrapper，并把结尾统一成独立的 `End`。

保留未动的模糊点有两类：`Joey's voice, but she sees Ross:` / `Joey's voice/Ross:` 这类 speaker 标记带解释成分，改动会带入判断；另外像普通动作行与场景提示的边界不够确定的地方，除非是明显切场，我都没有强行改。